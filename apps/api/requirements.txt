fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pydantic>=2.11.0
python-dotenv>=1.0.1
# Anthropic powers all agents + workflows (planner tool runner, sub-agents,
# server-side web search). The bare openai package remains ONLY for voice-input
# transcription (Whisper) — Anthropic has no transcription API.
anthropic>=0.116.0
openai>=1.50.0
python-docx>=1.1.0
python-pptx>=1.0.0
google-api-python-client>=2.150.0
google-auth>=2.36.0
google-auth-oauthlib>=1.2.1

# read_url (URL-fetch-and-extract): httpx is already pulled in by openai-agents
# but pinned here explicitly since url_fetch_service depends on it directly.
httpx>=0.27.0
trafilatura>=2.0.0      # primary main-content extractor
beautifulsoup4>=4.12.0  # fallback extractor (uses lxml when available)

# PDF-as-source (upload a PDF, extract its text to ground a run)
pypdf>=5.0.0
python-multipart>=0.0.9  # FastAPI UploadFile (PDF upload endpoints)
