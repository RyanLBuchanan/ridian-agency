# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Local settings (contains SMTP password)
apps/api/local_settings.json

# Per-machine sidebar "hidden runs" filter (not secrets, just user preference)
apps/api/hidden_projects.json

# Ridian Command Center local memory state (contacts, brand, facts, follow-ups, decisions)
apps/api/state/

# Google Drive OAuth (never commit either file)
apps/api/google_credentials.json
apps/api/google_token.json

# NotebookLM Enterprise OAuth spike (never commit either file)
apps/api/notebooklm_credentials.json
apps/api/notebooklm_token.json
spike_overview.mp3
# Throwaway investigation scripts (e.g. the deleted NotebookLM audio spike)
spike_*.py

# Outputs (generated artifacts)
outputs/*/
!outputs/.gitkeep

# Electron desktop GUI
desktop/node_modules/
desktop/dist/
desktop/out/

# Editors
.vscode/
.idea/
.DS_Store

apps/api/quickbooks_token.json
